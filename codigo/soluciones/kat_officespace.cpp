#include <bits/stdc++.h>
using namespace std;

signed main() {
    cin.sync_with_stdio(false);
    cin.tie(nullptr);
    int w, h;
    while (cin >> w >> h) {
        int n;
        cin >> n;
        vector<vector<int>> office(h, vector<int>(w));
        while (n--) {
            string name;
        }
    }
    cout.flush();
    return 0;
}
