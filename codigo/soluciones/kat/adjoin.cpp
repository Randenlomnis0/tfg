#include <bits/stdc++.h>
using namespace std;
#define FOR(i,j,k) for (int i = j; i < k; ++i)
signed main() {
    cin.sync_with_stdio(false);
    cin.tie(nullptr);
    int c, l;
    cin >> c >> l;
    FOR(i, 0, l) {
        int a, b;
        cin >> a >> b;
    }
    cout.flush();
    return 0;
}
