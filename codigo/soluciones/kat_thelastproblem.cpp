#include <bits/stdc++.h>
using namespace std;
#define GL(s) getline(cin, s)
signed main() {
    cin.sync_with_stdio(false);
    cin.tie(nullptr);
    string s;
    GL(s);
    cout << "Thank you, " + s + ", and farewell!\n";
    cout.flush();
    return 0;
}
